bots:
  test-bot:
    graph: notify-graph
actions:
  notify:
    steps:
      - webhook:
          url: "https://example.com/hook"
graphs:
  notify-graph:
    root: trigger
    nodes:
      trigger:
        action: notify
        edges: []
