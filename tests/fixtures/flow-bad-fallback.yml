bots:
  test-bot:
    flows:
      - id: broken-flow
actions:
  greet:
    reply: "Hello!"
flows:
  broken-flow:
    entry_step: start
    fallback_step: nonexistent
    steps:
      start:
        action: greet
        branches: []
