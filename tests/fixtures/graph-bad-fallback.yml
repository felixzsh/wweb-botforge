bots:
  test-bot:
    graph: broken-graph
actions:
  greet:
    reply: "Hello!"
graphs:
  broken-graph:
    root: start
    fallback: nonexistent
    nodes:
      start:
        action: greet
        edges: []
